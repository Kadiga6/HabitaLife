# 📐 Architecture - Diagramme UML

## Diagramme de classes - Paiements

```
┌─────────────────────────────────────┐
│         Utilisateur                  │
├─────────────────────────────────────┤
│ - id: int                           │
│ - email: string                     │
│ - prenom: string                    │
│ - nom: string                       │
│ - motDePasse: string                │
├─────────────────────────────────────┤
│ + getContrats(): Collection         │
│ + getEmail(): string                │
└─────────────────────────────────────┘
           ▲
           │ 1
           │
           │ * owns
           │
┌─────────────────────────────────────┐
│         Contrat                      │
├─────────────────────────────────────┤
│ - id: int                           │
│ - dateDebut: DateTime               │
│ - dateFin: DateTime (nullable)      │
│ - montantLoyer: decimal             │
│ - montantCaution: decimal           │
│ - statut: string                    │
├─────────────────────────────────────┤
│ + getUtilisateur(): Utilisateur     │
│ + getLogement(): Logement           │
│ + getMontant(): Collection<Paiement>│
└─────────────────────────────────────┘
           ▲
           │ 1
           │
           │ * contains
           │
┌─────────────────────────────────────┐
│         Paiement                     │
├─────────────────────────────────────┤
│ - id: int                           │
│ - periode: string                   │
│ - montant: decimal                  │
│ - datePaiement: DateTime (nullable) │
│ - moyenPaiement: string (nullable)  │
│ - statut: string                    │
│ - dateCreation: DateTime (nullable) │
├─────────────────────────────────────┤
│ + getContrat(): Contrat             │
│ + getPeriode(): string              │
│ + getMontant(): string              │
│ + getStatut(): string               │
│ + setStatut(string): void           │
└─────────────────────────────────────┘
           ▲
           │
           │
┌─────────────────────────────────────┐
│         Logement                     │
├─────────────────────────────────────┤
│ - id: int                           │
│ - adresse: string                   │
│ - ville: string                     │
│ - codePostal: string                │
│ - typeLogement: string              │
├─────────────────────────────────────┤
│ + getContrats(): Collection         │
│ + getAdresse(): string              │
└─────────────────────────────────────┘
```

## Flux de traitement - Page Paiements

```
┌──────────────────────┐
│  Utilisateur accède  │
│  à /payments         │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────────────────┐
│   PaymentsController::index()     │
│  Vérifie authentification          │
│  Récupère utilisateur connecté     │
└──────────┬───────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│   ContratRepository::findBy()     │
│  Récupère contrats de l'utilisateur
└──────────┬───────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│  PaiementRepository::findByIds()  │
│  Récupère paiements des contrats  │
└──────────┬───────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│  Calcul des statistiques          │
│  - Payés (count)                  │
│  - En attente (count)             │
│  - En retard (count)              │
└──────────┬───────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│   Rendre template                 │
│   payments/index.html.twig        │
│   avec données                    │
└──────────┬───────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│  Affichage du tableau des         │
│  paiements avec boutons "Payer"   │
└──────────────────────────────────┘
```

## Flux de traitement - Page de paiement

```
┌──────────────────────────────────┐
│  Utilisateur clique sur "Payer"   │
│  /payments/{id}/pay GET           │
└──────────┬───────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│  PaymentsController::pay(GET)     │
│  Récupère paiement par ID         │
└──────────┬───────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│  Vérification droits d'accès      │
│  Paiement.contrat.utilisateur     │
│  === utilisateur connecté ?       │
└──────────┬──────────┬────────────┘
           │          │
         OUI         NON
           │          │
           │          ▼
           │    ┌──────────────────┐
           │    │  AccessDenied    │
           │    │  Exception       │
           │    └──────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│  Créer formulaire PaiementFormType│
└──────────┬───────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│  Rendre template                 │
│  payments/pay.html.twig          │
│  avec formulaire et récapitulatif │
└──────────────────────────────────┘

           ║
    Utilisateur remplit le formulaire
           ║
           ▼

┌──────────────────────────────────┐
│  Utilisateur clique "Valider"    │
│  /payments/{id}/pay POST          │
└──────────┬───────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│  PaymentsController::pay(POST)    │
│  Traite le formulaire             │
└──────────┬───────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│  Validation du formulaire         │
│  Mode de paiement obligatoire ?   │
└──────────┬──────────┬────────────┘
           │          │
         OK        ERREUR
           │          │
           │          ▼
           │    ┌──────────────────┐
           │    │ Ré-afficher      │
           │    │ le formulaire    │
           │    │ avec erreurs     │
           │    └──────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│  Mise à jour du paiement :        │
│  - statut = "paye"               │
│  - datePaiement = now()          │
│  - moyenPaiement = sélection     │
│  - dateCreation = now()          │
└──────────┬───────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│  entityManager->flush()           │
│  Enregistre en base de données    │
└──────────┬───────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│  Ajouter message flash success    │
│  "Paiement effectué avec succès !"│
└──────────┬───────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│  Redirection vers /payments       │
└──────────────────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│  Affichage du message flash       │
│  Tableau mis à jour               │
│  (statut = "paye")               │
└──────────────────────────────────┘
```

## Architecture des fichiers

```
HabitaLife/
│
├─ src/
│  ├─ Controller/
│  │  └─ PaymentsController.php          ← Routes + Logique métier
│  │
│  ├─ Entity/
│  │  └─ Paiement.php                    ← Modèle de données
│  │
│  ├─ Form/
│  │  └─ PaiementFormType.php            ← Formulaire
│  │
│  └─ Repository/
│     └─ PaiementRepository.php          ← Requêtes BD
│
├─ templates/
│  └─ payments/
│     ├─ index.html.twig                 ← Liste paiements
│     └─ pay.html.twig                   ← Formulaire paiement
│
├─ migrations/
│  └─ Version20260120UpdatePaiement.php  ← Migration BD
│
└─ sql/
   └─ test_paiements.sql                 ← Données de test
```

## Cycle de vie d'un paiement

```
        ┌──────────────┐
        │  CRÉÉ        │
        │ (en_attente) │
        └──────┬───────┘
               │
        ┌──────▼──────────┐
        │  UTILISATEUR    │
        │  EFFECTUE       │
        │  PAIEMENT       │
        └──────┬──────────┘
               │
        ┌──────▼──────────┐
        │  PAYÉ           │
        │ (statut=paye)   │
        │ + datePayement  │
        │ + moyenPayement │
        └─────────────────┘

        OU (cas retard)

        ┌──────────────┐
        │  CRÉÉ        │
        │ (en_attente) │
        └──────┬───────┘
               │
        ┌──────▼──────────────┐
        │  DELAI DEPASSÉ      │
        │  (1er du mois)      │
        └──────┬──────────────┘
               │
        ┌──────▼──────────┐
        │  EN RETARD      │
        │ (en_retard)     │
        └──────┬──────────┘
               │
        ┌──────▼──────────────┐
        │  UTILISATEUR        │
        │  REGULARISE         │
        └──────┬──────────────┘
               │
        ┌──────▼──────────┐
        │  PAYÉ           │
        │ (statut=paye)   │
        │ + datePayement  │
        │ + moyenPayement │
        └─────────────────┘
```

## Relations dans la base de données

```
UTILISATEUR (1) ─────────── (N) CONTRAT
    id                          id
    email                       utilisateur_id ─────┐
    prenom                      logement_id         │
    nom                         date_debut          │
                                montant_loyer       │
                                                    │
                            (1) CONTRAT ─────────── (N) PAIEMENT
                                 id                     id
                                                        contrat_id ─────┐
                                                        periode         │
                                                        montant         │
                                                        date_paiement   │
                                                        moyen_paiement  │
                                                        statut          │

LOGEMENT (1) ─────────── (N) CONTRAT
    id                       id
    adresse                  logement_id ─────┐
    ville                                     │
    code_postal                               │
    type_logement                             │
```

---

**Note :** Ce diagramme représente la structure technique. La logique métier est gérée au niveau du contrôleur et du service.
