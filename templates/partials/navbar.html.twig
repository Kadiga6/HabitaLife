{# Navbar SaaS premium - Style minimaliste #}
<nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom shadow-sm sticky-top">
    <div class="container-fluid px-4">
        {# Logo / Brand #}
        <a class="navbar-brand d-flex align-items-center" href="{{ path('dashboard') }}">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" class="me-2">
                <path d="M12 3L3 9V21H21V9L12 3Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
                <path d="M9 21V12H15V21" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
            <span class="fw-semibold text-dark fs-5">HabitaGo</span>
        </a>

        {# Toggle button pour mobile #}
        <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button> {# Navigation principale #}
        <div class="collapse navbar-collapse" id="navbarContent">
            <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link px-3 {{ app.request.attributes.get('_route') starts with 'dashboard' ? 'active' : '' }}" href="{{ path('dashboard') }}">
                        Dashboard
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link px-3 {{ app.request.attributes.get('_route') starts with 'payments' ? 'active' : '' }}" href="{{ path('payments') }}">
                        Paiements
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link px-3 {{ app.request.attributes.get('_route') starts with 'consumption' ? 'active' : '' }}" href="{{ path('consumption') }}">
                        Consommations
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link px-3 {{ app.request.attributes.get('_route') starts with 'issues' ? 'active' : '' }}" href="{{ path('issues') }}">
                        Incidents
                    </a>
                </li>
            </ul>

            {# Actions utilisateur #}
            <div class="d-flex align-items-center gap-2">


                {# Dropdown compte utilisateur #}
                <div class="dropdown">
                    <button class="btn btn-light dropdown-toggle d-flex align-items-center gap-2 border-0" type="button" id="userMenu" data-bs-toggle="dropdown" aria-expanded="false">
                        <div class="avatar-circle">
                            <span class="avatar-initials">MJ</span>
                        </div>
                        <span class="d-none d-md-inline">Marie</span>
                    </button>
                    <ul class="dropdown-menu dropdown-menu-end shadow" aria-labelledby="userMenu">
                        <li>
                            <a class="dropdown-item" href="{{ path('profile') }}">
                                <i class="bi bi-person me-2"></i>Mon profil
                            </a>
                        </li>
                        <li>
                            <a class="dropdown-item" href="{{ path('settings') }}">
                                <i class="bi bi-gear me-2"></i>Paramètres
                            </a>
                        </li>
                        <li>
                            <hr class="dropdown-divider">
                        </li>
                        <li>
                            <a class="dropdown-item text-danger" href="#" onclick="return false;">
                                <i class="bi bi-box-arrow-right me-2"></i>Se déconnecter
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</nav>