<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}HabitaGo{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>"> {# Bootstrap 5 CSS #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('css/app.css') }}">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <link rel="icon" href="{{ asset('favicon.ico') }}"> {# Custom CSS pour navbar SaaS premium #}
    <style>
        /* Typographie moderne */
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            color: #212529;
        }
        /* Navbar styles */
        
        .navbar {
            height: 64px;
            backdrop-filter: blur(10px);
            transition: all 0.3s ease;
        }
        
        .navbar-brand {
            font-weight: 600;
            letter-spacing: -0.02em;
            color: #1a1a1a !important;
            transition: opacity 0.2s ease;
        }
        
        .navbar-brand:hover {
            opacity: 0.8;
        }
        
        .navbar-brand svg {
            color: #0d6efd;
        }
        /* Navigation links */
        
        .nav-link {
            color: #6c757d !important;
            font-weight: 500;
            font-size: 0.9375rem;
            position: relative;
            transition: color 0.2s ease;
        }
        
        .nav-link:hover {
            color: #212529 !important;
        }
        
        .nav-link.active {
            color: #0d6efd !important;
        }
        
        .nav-link.active::after {
            content: '';
            position: absolute;
            bottom: -1px;
            left: 50%;
            transform: translateX(-50%);
            width: 24px;
            height: 2px;
            background-color: #0d6efd;
            border-radius: 2px 2px 0 0;
        }
        /* Avatar circle */
        
        .avatar-circle {
            width: 32px;
            height: 32px;
            border-radius: 50%;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .avatar-initials {
            color: white;
            font-size: 0.75rem;
            font-weight: 600;
            letter-spacing: 0.5px;
        }
        /* Dropdown menu */
        
        .dropdown-menu {
            border: 1px solid #e9ecef;
            border-radius: 0.5rem;
            padding: 0.5rem;
            margin-top: 0.5rem;
            min-width: 200px;
        }
        
        .dropdown-item {
            border-radius: 0.375rem;
            padding: 0.5rem 0.75rem;
            font-size: 0.9375rem;
            transition: all 0.2s ease;
        }
        
        .dropdown-item:hover {
            background-color: #f8f9fa;
        }
        
        .dropdown-item.text-danger:hover {
            background-color: #fff5f5;
        }
        /* Button styles */
        
        .btn-light {
            background-color: #f8f9fa;
            border: 1px solid #e9ecef;
            border-radius: 0.5rem;
            padding: 0.375rem 0.75rem;
            font-weight: 500;
            transition: all 0.2s ease;
        }
        
        .btn-light:hover {
            background-color: #e9ecef;
            border-color: #dee2e6;
        }
        /* Badge notification */
        
        .badge {
            font-weight: 600;
        }
        /* Responsive adjustments */
        
        @media (max-width: 991.98px) {
            .navbar-nav {
                padding-top: 1rem;
            }
            .nav-link.active::after {
                display: none;
            }
            .nav-link.active {
                background-color: #f8f9fa;
                border-radius: 0.375rem;
            }
        }
        /* Main content spacing */
        
        main {
            min-height: calc(100vh - 64px);
            background-color: #f8f9fa;
        }
    </style>

</head>

<body>
{# Navbar #}
{% if app.request.attributes.get('_route') not in ['app_login', 'app_register'] %}
    {% include 'partials/navbar.html.twig' %}
{% endif %}
    {# Messages flash #}
    <div class="container-lg mt-3">
        {% for type, messages in app.flashes %}
            {% if type == 'success' %}
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    <i class="bi bi-check-circle-fill me-2"></i>
                    {% for message in messages %}
                        <strong>{{ message }}</strong>
                    {% endfor %}
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            {% elseif type == 'error' %}
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    <i class="bi bi-exclamation-triangle-fill me-2"></i>
                    {% for message in messages %}
                        <strong>{{ message }}</strong>
                    {% endfor %}
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            {% elseif type == 'warning' %}
                <div class="alert alert-warning alert-dismissible fade show" role="alert">
                    <i class="bi bi-exclamation-circle-fill me-2"></i>
                    {% for message in messages %}
                        <strong>{{ message }}</strong>
                    {% endfor %}
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            {% endif %}
        {% endfor %}
    </div>

    {# Contenu principal #}
    <main class="container-fluid py-4">
        {% block body %}{% endblock %}
    </main>

    {# Bootstrap 5 JS Bundle #}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>